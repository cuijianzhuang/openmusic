# OpenMusic AI 协作与交付规范

> **适用范围**：本文件是 OpenMusic 仓库中 AI、自动化代理及开发协作者的最高优先级执行指南。开始分析、修改、测试、发布建议或提交前必须完整阅读。若目标目录存在更具体的 `AGENTS.md`，以距离目标文件最近的规则为准；不得与本文件的安全、质量和兼容性要求冲突。

## 0. 快速协作卡

把一次改动当作一份可交接的小型交付，而不只是一次代码编辑。开始前先用下面这张卡片校准工作：

| 时点 | 必做动作 | 产出标准 |
|---|---|---|
| 接到任务 | 写清目标、非目标、用户可见变化和风险等级 | 需求里的隐含权限、数据或兼容性问题被显式指出 |
| 打开仓库 | 运行 `git status --short`，再用 `rg` 查入口、调用方、配置、协议和测试 | 知道哪些未提交内容不能触碰，知道改动的读写双方 |
| 动手前 | 列出将改的文件、兼容策略、失败表现和回滚方式 | L2/L3 改动还需说明旧客户端、旧数据和重试/并发行为 |
| 交付前 | 运行与风险相符的检查，并如实记录结果 | 未运行、失败或无法覆盖的项必须保留在交付说明中 |

**最小交接说明**：当任务信息不足但可能影响安全、数据或协议时，先问一个能解除风险的具体问题；其余情况下做保守假设并在交付中写明。不要用“已完成”代替验收标准，也不要把未验证推断写成事实。

## 1. 目标与不可妥协原则

OpenMusic 是多人实时在线点歌系统，包含 Web、Node.js 服务端、Redis、Socket.IO、外部音乐服务、管理后台和 Flutter 移动端。每一次变更都必须同时满足：正确性、安全性、可观测性、可回退性和兼容性。

1. **先理解，后修改**：先阅读关联实现、调用方、数据流、配置、测试和文档；禁止基于局部猜测直接改代码。
2. **最小且完整**：变更范围要小，但必须覆盖受影响的读写双方、权限路径、错误路径和文档。不得借任务顺带重构或格式化无关文件。
3. **服务端最终裁决**：鉴权、权限、限流、输入校验、数据完整性和敏感操作必须由服务端执行；前端隐藏或禁用控件不构成安全控制。
4. **不破坏既有数据和客户端**：HTTP API、Socket.IO 事件、Redis 键、持久化 JSON、环境变量、Cookie、localStorage 和移动端协议均为兼容性边界。
5. **不伪造结论**：未运行的测试、构建、手工验证、性能验证或安全验证必须明确说明；不得声称“已验证”。
6. **保护现有工作区**：未提交内容默认属于用户或其他协作者。除非用户明确授权，不得还原、删除、覆盖、暂存或提交无关变更。

## 2. 项目边界与责任地图

| 路径 | 责任 | 关键约束 |
|---|---|---|
| `client/` | React + Vite Web 客户端 | TypeScript；页面位于 `src/pages/`，状态位于 `src/stores/`，通用能力在 `src/lib/`。修改请求、事件或配置必须同步服务端。普通用户主站禁止引入 Ant Design。 |
| `server/` | Express + Socket.IO 业务服务 | `index.js` 是 HTTP、实时事件和启动编排入口；Redis 是生产必需依赖。禁止以改变生产语义的内存兜底代替 Redis。 |
| `mobile/` | Flutter WebView 移动壳与 Android 原生媒体能力 | 与 Web 共用后端协议；`mobile/WEB_SHELL.md` 定义网页到原生的桥接契约。涉及登录、房间、播放、队列、聊天或 Socket 事件必须评估兼容性。 |
| `docs/` | 部署、移动端、用户和运维文档 | 用户可见功能、配置、命令、环境变量、部署或故障处理变更必须同步。 |
| `deploy/`、`Dockerfile`、`docker-compose*.yml` | 镜像、反代和运行编排 | 端口、卷、健康检查、启动顺序、反代路径和环境变量均是生产契约。 |
| `data/`、`server/*.json`、`server/.env` | 运行时持久化状态 | 不提交真实生产数据、密钥、凭据、下载物或本地状态。 |
| `.github/workflows/` | CI 与发布自动化 | 不得弱化检查以让构建通过；变更脚本或依赖后要同步评估 CI。 |

## 3. 变更分级与执行门禁

| 等级 | 典型范围 | 实施前最低要求 | 合并/交付前最低要求 |
|---|---|---|---|
| L0 低风险 | 文案、注释、纯文档 | 核对引用路径和事实 | `git diff --check`；验证链接与命令 |
| L1 常规 | 单端 UI、独立工具函数、非敏感逻辑 | 搜索调用方与类型 | 对应类型检查/构建；覆盖关键成功与失败路径 |
| L2 高影响 | HTTP/Socket 协议、Redis、配置、播放器、房间规则、管理后台 | 明确兼容策略、权限和回滚方式 | 前后端/移动端影响核对；测试或手工场景记录；文档同步 |
| L3 高风险 | 身份、密钥、Cookie、OAuth、支付/捐赠、外部代理、生产部署、数据迁移、AI 工具执行 | 书面列出威胁、权限、数据和回滚影响；不确定时先询问 | 安全审查、最小权限、脱敏验证、回滚步骤和上线后观测项齐备 |

以下变更默认按 L2 或更高处理：公共接口、Socket.IO 事件、Redis 数据格式或 TTL、运行时配置、管理员能力、房间权限、播放状态、外部 URL 请求、上传、下载、AI 模型和部署。

## 4. 标准工作流

1. **确认任务**：提炼目标、非目标、用户可见行为、风险等级和验收标准。需求不明确但会影响安全、数据或兼容性时，先提出最小必要问题。
2. **建立上下文**：先运行 `git status --short`；使用 `rg` 查找入口、调用方、类型、事件名、配置字段、Redis 键和测试；阅读相关 README/`docs/`。
3. **设计变更**：说明将改哪些文件、哪些契约受影响、兼容策略、失败处理和回滚方法。L2/L3 必须优先处理旧客户端、旧数据和并发重试。
4. **实施**：遵循现有风格和模块边界。优先复用已有校验、鉴权、配置和错误处理，不复制粘贴业务规则。
5. **验证**：执行与风险匹配的自动化检查；必要时启动服务进行关键链路验证。UI 改动检查桌面和窄屏；实时功能检查重连与多用户状态。
6. **交付说明**：列出修改内容、验证命令及结果、未验证项、剩余风险、配置/迁移/回滚事项。不得隐藏失败或警告。

## 5. 接口、事件与数据契约

### HTTP API

- 新增或修改接口前，定义并实现：认证来源、授权规则、输入模式与长度限制、限流、状态码、稳定错误码/错误信息、响应脱敏和日志边界。
- 公共字段遵循“只追加、先兼容后移除”。删除、改名、改变类型或默认语义属于破坏性变更，必须提供版本化、兼容读取或灰度迁移。
- 所有外部输入均不可信，包括请求体、查询参数、请求头、Cookie、上传元数据和第三方回调。
- `GET /api/health` 当前只表示服务进程可响应；未实现的依赖深度健康检查不得在文档或交付说明中虚构为已有能力。

### Socket.IO

- 每个事件必须明确：发起端、服务端鉴权、参数校验、幂等/重复提交策略、成功回调、失败回调、广播房间及断线重连行为。
- 事件名称和 payload 是跨 Web/移动端的公共协议。修改前搜索全部发送方与监听方；无法同时升级的客户端必须有兼容分支或安全降级。
- 广播前确认房间隔离与权限范围，禁止将房间私密信息、管理员信息、账号凭据或内部错误广播给无关用户。

### WebView 与原生媒体桥接

- `mobile/WEB_SHELL.md` 中的状态字段、命令名与权限字段是 Web、Flutter、Android 之间的公共协议；修改前必须同步搜索三端发送方与监听方。
- 原生端发起的播放、下一首、切换模式等动作只是一条请求，网页和服务端仍必须做现有权限校验；不得把通知栏、悬浮歌词或 WebView 回调视为可信授权来源。
- 悬浮歌词需要 Android 的悬浮窗权限。权限被拒绝、桥接不可用或原生服务失败时，网页播放与房间基础功能必须继续可用。

### 前端依赖边界与包体积

- `antd` 与 `@ant-design/*` 属于大型依赖，只允许出现在管理后台边界：`client/src/pages/admin/`、`client/src/pages/Admin.tsx` 和 `client/src/pages/Setup.tsx`。
- `client/src/pages/Home.tsx`、`Room.tsx`、`TvDisplay.tsx`、普通用户组件、hooks、stores、lib 及其他主站代码禁止直接或间接新增 Ant Design 引入，避免把管理后台依赖带入普通用户首屏或主站 chunk。
- 普通用户页面优先使用原生 HTML、项目已有轻量组件或 `lucide-react` 图标。新增 UI 依赖前必须评估构建产物、首屏加载和代码分割影响。
- 不得通过修改检查脚本、扩大允许目录或隐藏 import 的方式绕过此规则；确需使用 Ant Design 时，必须将功能移动到管理后台边界并保持懒加载。

### Redis 与运行时状态

- Redis 是房间、收藏、凭据、公告、封禁和管理数据的重要持久化依赖。键名、TTL、序列化格式、索引关系和清理逻辑均为数据模型的一部分。
- 数据结构变更必须实现“新写可读、旧读可用”的迁移策略；禁止直接假设存量数据已升级。涉及批量迁移需具备幂等性、进度记录、失败恢复和回滚方案。
- `server/runtimeConfig.js`、`server/adminConfig.js`、`server/adminApi.js` 是配置边界。新增配置必须有安全默认值、类型/范围校验、脱敏输出、审计考虑和向后兼容。
- `runtimeConfig.json`、`adminConfig.json`、`setup.lock`、`.env`、管理员凭据、下载文件和 `data/` 均为运行时数据，不得作为源码变更提交。

## 6. 安全、隐私与 AI 专项要求

### 凭据与身份

- API Key、Cookie、扫码会话、OAuth token/client secret、管理员凭据、Redis 密码和用户身份信息只能在服务端按最小范围使用；禁止写入前端包、URL、日志、错误响应、测试快照或提交记录。
- 生产环境的 `CLIENT_ID_SECRET`、房间凭证密钥等必须稳定持久。不得为了启动成功自动轮换已有密钥，否则会导致既有会话或加密数据不可恢复。
- 不得关闭 HTTPS 校验、请求签名、会话校验、CSRF/回调 state 校验、IP 限制或权限判断来“修复”联调问题。调试开关必须默认关闭且不可作为生产方案。

### 网络、代理与上传

- 对外请求必须有目标校验、协议限制、超时、大小限制、错误处理和 SSRF 防护；不得允许访问回环、内网、链路本地或云元数据地址。
- 媒体代理、文件上传和外链处理必须校验来源、类型、大小、重定向和响应头。不得把用户提供的 URL 直接透传为可信请求。
- 不在日志中记录完整查询参数、Cookie、Authorization、上传签名或第三方响应原文；排障时使用掩码和最小必要字段。

### AI 功能

- AI 输入包含不可信提示词、聊天内容和图片。系统提示、工具定义、权限边界和服务端校验优先于模型输出；模型不得自行获得管理员、房主或跨房间权限。
- AI 触发的点歌、队列、播放或房间操作必须复用服务端既有权限、限流、审计和参数校验，不得因“工具调用”绕开普通用户路径。
- 模型 API Key 只能保存在服务端加密配置中；管理后台和日志仅展示脱敏信息。模型、供应商、限流或视觉能力不可用时必须安全失败，不影响基础房间功能。
- 不向第三方模型发送不必要的 Cookie、账号凭据、管理员信息、内部配置或其他房间的内容。需要记录对话时遵循最小化、隔离和可清理原则。

## 7. 领域规则与并发控制

- 房主、管理员、成员、访客、被禁言/封禁用户的权限必须在服务端逐操作验证。
- 房间、播放、队列、收藏、成员关系和聊天是并发状态。任何读改写操作都要考虑竞态、重复回调、断线重连、消息乱序和服务重启。
- 修改自动播放、插队、清空队列、转让房主、解散房间、共享账号、封禁或身份恢复时，必须定义失败后的最终状态和用户可见反馈。
- 外部音乐源、Meting、OAuth、七牛等依赖应保留现有超时、错误转换、限流和降级语义；对上游短暂失败不得污染本地持久状态。

## 8. 质量门禁与验证矩阵

### 仓库已有自动化门禁

CI 对 `main`、`develop` 推送及 Pull Request 执行：前端 `npm run build:check`；服务端全部 JavaScript 语法检查和 `npm test`。本地按需运行：

```bash
npm run typecheck                 # 前端 TypeScript
npm run build:check               # 类型检查、Ant Design 边界、生产构建
npm run build                     # 前端生产构建
npm test --prefix server           # 服务端测试
npm run dev                       # Web :5173，服务端 :4000
curl http://localhost:4000/api/health
```

### 按变更类型追加验证

| 变更类型 | 至少验证 |
|---|---|
| 前端 UI/状态 | 类型检查、生产构建、桌面与窄屏关键流程、加载/空态/失败态 |
| 前端依赖/组件 | `npm run build:check`；确认 `check:antd-boundary` 通过；检查主站 chunk 未引入 `antd-vendor` |
| HTTP/Socket | 成功、未认证、越权、非法参数、限流、断线重连、旧 payload 或客户端兼容 |
| Redis/配置 | 默认值、旧配置读取、写入后重启、无效输入、敏感字段脱敏、失败恢复 |
| 权限/身份/凭据 | 正向权限、越权拒绝、会话失效、日志与响应脱敏、回调伪造/重放边界 |
| AI/外部依赖 | 未配置、超时、限流、供应商错误、工具越权、敏感输入不外泄、基础功能不受影响 |
| Docker/部署 | 镜像构建、容器启动、卷持久化、Redis 健康依赖、`/api/health`、反代 WebSocket 路径 |
| 移动端协议 | Flutter 侧调用点核对；无法实测时在交付中明确说明覆盖范围与风险 |

## 9. 发布、回滚与可观测性

- 发布前记录：版本/提交、变更摘要、风险等级、配置变化、迁移步骤、验收结果、回滚触发条件和负责人。
- 涉及前后端协议或配置语义时遵循顺序：先部署可兼容的服务端，再部署客户端；删除旧字段或旧行为必须在兼容窗口结束后单独执行。
- 修改 Docker、Nginx、端口、卷或环境变量时，必须说明重启需求、持久化影响与恢复步骤。`docker compose` 中 Redis 使用 AOF 和卷，但生产备份策略需由运维明确配置，不可假设已具备异地备份。
- 服务端已实现优雅退出与 Redis 必需依赖校验。变更启动/退出链路时不得破坏 Socket 客户端重连、房间刷写和容器 `stop_grace_period` 的配合。
- 新增关键功能至少定义可观察信号：成功量、失败量、拒绝/限流量、上游错误或关键延迟。当前项目未声明集中式监控平台；不要把 `console` 输出描述为完整监控或告警系统。

## 10. 代码与文档准则

- 遵循现有模块划分、命名、错误返回和 UI 风格。优先使用已存在的工具函数、store、类型和配置入口。
- 代码注释只解释难以从代码本身得出的约束、原因或风险；不要添加重复代码表意的注释。
- 新增依赖、升级 Node、迁移框架或修改构建链路属于高影响变更，必须说明必要性、许可证/安全影响、锁文件变化及 CI/Docker 影响。
- 用户可见功能、部署步骤、环境变量、运维命令或安全语义变化时，同步更新 `README.md`、`docs/DEPLOY.md`、`docs/MOBILE.md` 或相应文档。
- 当项目结构、验证命令、协议或关键风险变化时，必须同步维护本文件。

## 11. 明确禁止事项

- 禁止提交 `.env`、真实 API Key、Cookie、OAuth 密钥、二维码登录会话、用户数据、运行时 JSON、日志、构建产物、`node_modules/` 或 `data/` 内容。
- 禁止使用 `git reset --hard`、`git checkout --` 或批量删除命令覆盖现有工作，除非用户已明确指定目标和范围。
- 禁止通过关闭类型检查、删除授权、放宽 CORS、禁用 HTTPS 校验、吞掉异常、跳过限流或永久开启调试开关来消除报错。
- 禁止把密钥、账号凭据、私有接口地址或内部错误详情暴露给前端、普通房间成员、第三方模型或无关日志。
- 禁止在没有兼容和回滚策略时直接修改公共协议、持久化结构、加密密钥、管理员入口或生产部署架构。

## 12. AI 最终交付模板

完成任务时，必须给出以下信息；无内容时写“无”或说明原因：

1. **变更摘要**：修改了什么，以及对应的用户/系统行为。
2. **影响范围**：涉及的客户端、服务端、移动端、数据、配置、部署或文档。
3. **验证结果**：实际执行的命令、关键手工场景与结果。
4. **风险与未验证项**：包括无法访问的依赖、未运行的环境和兼容性假设。
5. **上线与回滚**：需要的配置、迁移、发布顺序、观测信号与回退方式；无则明确“无”。

交付信息应以可执行事实为主：列出实际运行过的命令与结果，而不是笼统写“测试通过”；涉及协议或配置时，明确先后部署顺序与回退触发条件。
