# OpenMusic iOS

Run `flutter create --platforms=ios .` inside `mobile/` after installing Flutter SDK
to regenerate full Xcode project if needed. Minimal placeholders live here for CI docs.

Required Info.plist keys (audio background):
- UIBackgroundModes: audio
- NSAppTransportSecurity as needed for your server
